package com.example.etfbuyalert.ui.theme

import androidx.compose.ui.graphics.Color

// ダークモード用の色（黒基調：背景は純黒、面は黒寄りグレーで階層化）
val DarkBackground = Color(0xFF000000)        // 背景：純黒
val DarkSurface = Color(0xFF121212)           // カード
val DarkSurfaceVariant = Color(0xFF1E1E1E)    // 一段上のカード

val AccentGreen = Color(0xFF4CAF50)           // 緑は差し色として残す（ボタン・見出し・選択タブ）
val AccentGreenDark = Color(0xFF388E3C)
val AccentGreenContainer = Color(0xFF242424)  // 色帯ヘッダー：緑帯をやめ黒寄りの濃グレーに

val TextPrimary = Color(0xFFE0E0E0)
val TextSecondary = Color(0xFF9E9E9E)
